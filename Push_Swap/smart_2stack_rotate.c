#include "push_swap.h"

void	smart_2stack_rotate(t_list **a, t_list **b, int *sorted, info *inf)
{
	set_moves(*a, *b, sorted, inf);
}

void	set_moves(t_list *a, t_list *b, int *sorted, info *inf)
{
	

